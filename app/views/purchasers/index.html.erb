<p id="notice"><%= notice %></p>

<h1>Listing Purchasers</h1>

<table>
  <thead>
    <tr>
      <th>Persona</th>
      <th>Cuil cuit</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @purchasers.each do |purchaser| %>
      <tr>
        <td><%= purchaser.nameSurname %></td>
        <td><%= purchaser.cuil_cuit %></td>
        <td><%= link_to 'Ver', purchaser %></td>
        <td><%= link_to 'Editar', edit_purchaser_path(purchaser) %></td>
        <td><%= link_to 'Eliminar
        ', purchaser, method: :delete, data: { confirm: '¿Seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Agregar', new_purchaser_path %>
